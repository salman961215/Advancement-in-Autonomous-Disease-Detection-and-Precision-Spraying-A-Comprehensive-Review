# Tables

Machine-readable resources derived from the paper.

- `datasets.csv` — the 27 datasets from Table 3 (images/samples, classes, crops, availability, type, link).
- `sati_scores.csv` — Sensor–Actuation Tightness Index component scores for the 9 scored coupled systems (Table 7). Reproduce the figure with `../scripts/plot_sati_scores.py`.
